# List of Bugs
> This document contains a list of known bugs in the project that i have found while doing manual testing.

1. When we use the "Format from clipboard" option, the formatting is applied and it opens the format view but it does not appear in the format history unless i press the format button.
2. When i increase the width of my VS Code window istance the two input / formatted panes do not resize accordingly, they stay the same size and an empty space appears on the right side of the window if i increase the width of the window on the right side of the window. it only adapts when i do a reload of the window.
